---
status: asserted
sources:
  - ../sources.kb/user.md
---

`grep`/`ls` tooling works fine at thousands-of-nodes scale too, so the
corpus-size forecast doesn't gate tooling choices. Dissolves
[expected-corpus-scale] rather than answering it: there is no
restructuring threshold to anticipate.

[expected-corpus-scale]: ../questions.kb/expected-corpus-scale.md
